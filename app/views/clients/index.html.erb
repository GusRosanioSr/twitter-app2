<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" lang="en" xm:lang="en">
    <head>
        <meta charset="utf-8"/>
        <title>Twitter Client Home</title>
        <link rel="stylesheet" href="home.css"/>
    </head>
    <body>
        <div class="maindiv">
            <h1>Search Twitter for latest Tweets on what you like!</h1>
        </div>
        <div class="twitter">
            <h2>Back to search page: <%= link_to 'Home', root_path %> </h2>
            <p>Showing results for : <%= @search %> </p>
            <%= @client.search(@search, result_type: "recent", lang: "en").take(10).collect do |tweet| %>
              <blockquote><h2>Username:</h2> <%= "#{tweet.user.screen_name}:" %></blockquote>
              <blockquote><h3>Tweet:</h3> <%= "#{tweet.full_text}" %></blockquote>
              <% end %>
        </div>
    </body>
</html>